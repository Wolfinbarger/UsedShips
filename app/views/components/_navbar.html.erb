<div class="container">
  <div class="d-flex justify-content-between">
    <div id = "border" class = "border border-info p-0" >
      <h1 class = "lines m-0 p-3">Blork Used Spaceship Emporium</h1>
    </div>
    <div id = "border" class = "border border-info w-25 text-center" >
<form novalidate="novalidate" class="simple_form search" action="/" accept-charset="UTF-8" method="get">
  <div class="search-form-control form-group">
    <input id = "border" class="form-control string required bg-transparent border border-info" placeholder = "Type here..." type="text" name="search[query]" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search text-color"></i>
    </button>
  </div>
</form>
    </div>
    <div class = "" >
      <% if user_signed_in? %>
        <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete} %>
      <% else %>
      <%= link_to "Login", new_user_session_path %>
      <% end %>
    </div>
  </div>
